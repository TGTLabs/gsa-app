<link rel="stylesheet" type="text/css" href="css/surveyStyle.css">


<div id="headerLogo"> <span>TELL </span>TARGET</div>


<!-- Banner -->
<section id="banner">


<div id="lightbox"></div>
<div id="lb-content">
     
<section id="banner">

    <div class="inner">
        <div id="bullseye-logo"><img src="images/Bullseye_75_CMYK.svg"></div>
        <header>
            <h2>Thank you</h2>
        </header>
        <p>For sharing your voice
            <br />

        <div class="navbar-social">
            <a alt="Share via Facebook" title="Share via Facebook" href="https://www.facebook.com/sharer/sharer.php?u=http://polls.targetrad.com" target="_blank"><img src="../images/ICN_faceSpace@2x.png"/></a>
            <a alt="Share via Twitter" title="Share via Twitter" href="https://twitter.com/intent/tweet?text=Check it out! Tell Target: an opportunity to shape Target's products & services by taking quick polls! http://polls.targetrad.com" target="_blank"><img src="../images/ICN_tweeter@2x.png"/></a>
            <a alt="Share via Email" title="Share via Email" href="mailto://?subject=Back%20to%20School%20List%20Assist&body=Check it out! Tell Target: an opportunity to shape Target's products & services by taking quick polls! http://polls.targetrad.com"><img src="../images/ICN_tehEmails@2x.png"/></a>
        </div>

    </div>

</section>  
</div>
    <script>
        $(document).ready(function () {

            var disp = function () {
                var lightBox = $('#lightbox');
                var lightBoxContent = $('#lb-content');
                var positionLightbox = function () {
                    var viewWidth = $(window).width();
                    var lbContentMargin = (viewWidth / 2) - 148;
                    var lbContent = $('#lb-content');

                    lbContent.css({
                        'left': lbContentMargin
                        //  'top' : $(window).scrollTop() + 50 + 'px'
                    });
                };
                $('#centerPolls').hide();
                lightBox.fadeIn(function () {
                    lightBoxContent.show();
                });
                positionLightbox();

                $('#lb-close').click(function () {
                    lightBox.hide();
                    lightBoxContent.hide();
                });
            };

            var target = $('.polar-polls');
            var polarOptions = {
							{{#if set}}
                  pollSet: {{id}},
							{{else}}
                  pollId: {{id}},
							{{/if}}

                publisher: 'TargetUXR',
                callbacks: {
                    vote: function (data) {
                        // this fires on both voting and unvoting actions
                        // data.pollID
                        // data.choice (1,2, etc)
                    },
                    loaded: function (data) {
                        // when the survey loads onto the page
                        // data.app = EmbeddedPollsApp
                        data.app.voteActionManager.displayShare = function (poll, vote, voteAction) {
                            disp();
                        };
                    },
                    navigate: function (data) {
                        // (PollSet and if there are multiple polls to navigate) this fires when the question transitions; both with navigation and when answering a question
                        // data.direction ('next', 'previous')
                        // data.currentPollID
                        // data.nextPollID
                    },
                    lastPoll: function () {
                        // (PollSet and if there are multiple polls to navigate)
                        // this only fires when the controls are engaged, not when answering questions
                    }
                }
            };

            //Instantiate new EmbeddedPollsApp showing a single Poll by ID
            var polar = new EmbeddedPollsApp(target, polarOptions);

            polar.init();
        });
    </script>
    <div id="centerPolls">
        <div class="polar-polls"></div>
    </div>

</section>
